<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Vitrine d'objets Roblox</title>
  <style>
    body { margin:0; font-family: system-ui, Arial; background: linear-gradient(#020617,#0f172a); color:#fff; }
    .container{ max-width:1100px; margin:auto; padding:24px; }
    h1{ font-size:28px; margin-bottom:6px; }
    p.subtitle{ color:#cbd5e1; }
    .search{ margin:20px auto; max-width:420px; }
    .search input{ width:100%; padding:10px 12px; border-radius:12px; border:1px solid #1e293b; background:#020617; color:#e5e7eb; }
    .grid{ display:grid; gap:18px; grid-template-columns:repeat(auto-fill,minmax(260px,1fr)); }
    .card{ background:#020617; border:1px solid #1e293b; border-radius:18px; box-shadow:0 12px 24px rgba(0,0,0,.25); overflow:hidden; }
    .card img{ width:100%; height:160px; object-fit:cover; }
    .card-content{ padding:14px 16px 18px; }
    .card h3{ margin:6px 0 6px; font-size:18px; }
    .card p{ color:#cbd5e1; font-size:14px; }
    .tags{ margin-top:8px; display:flex; flex-wrap:wrap; gap:6px; }
    .tag{ padding:4px 8px; border-radius:10px; background:#1e293b; font-size:12px; }
    .empty{ text-align:center; margin-top:32px; color:#94a3b8; }
    footer{ text-align:center; margin-top:40px; color:#64748b; font-size:12px; }
  </style>
</head>
<body>
  <div class="container">
    <header style="text-align:center; margin-bottom:18px;">
      <h1>Vitrine d'objets Roblox</h1>
      <p class="subtitle">Présente tes modèles et assets Roblox facilement ✨</p>
    </header>

    <div class="search">
      <input id="search" type="text" placeholder="Rechercher un objet ou un tag...">
    </div>

    <div id="items" class="grid"></div>

    <p id="empty" class="empty" style="display:none;">Aucun objet trouvé — essaie un autre mot-clé.</p>

    <footer>
      Créé pour mettre tes objets Roblox en valeur
    </footer>
  </div>

  <script>
    // 👉 Modifie / ajoute tes objets ici
    const items = [
      {
        title: "Épée légendaire",
        description: "Une arme rare avec effets lumineux.",
        image: "https://placehold.co/600x400?text=Objet+1",
        tags: ["arme", "rare"]
      },
      {
        title: "Voiture futuriste",
        description: "Véhicule rapide parfait pour maps de course.",
        image: "https://placehold.co/600x400?text=Objet+2",
        tags: ["véhicule", "course"]
      },
      {
        title: "Maison moderne",
        description: "Bâtiment moderne pour mondes roleplay.",
        image: "https://placehold.co/600x400?text=Objet+3",
        tags: ["bâtiment", "roleplay"]
      }
    ];

    const list = document.getElementById("items");
    const empty = document.getElementById("empty");
    const searchInput = document.getElementById("search");

    function render(data){
      list.innerHTML = data.map(item => `
        <div class="card">
          <img src="${item.image}" alt="${item.title}">
          <div class="card-content">
            <h3>${item.title}</h3>
            <p>${item.description}</p>
            <div class="tags">
              ${item.tags.map(t=>`<span class='tag'>${t}</span>`).join('')}
            </div>
          </div>
        </div>
      `).join("");
      empty.style.display = data.length ? "none" : "block";
    }

    function filter(){
      const q = searchInput.value.toLowerCase();
      const result = items.filter(i =>
        i.title.toLowerCase().includes(q) ||
        i.tags.some(t=>t.toLowerCase().includes(q))
      );
      render(result);
    }

    searchInput.addEventListener("input", filter);
    render(items);
  </script>
</body>
</html>
